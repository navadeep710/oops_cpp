#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    
    int a[n];
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
    
    int k;
    cin >> k;
    
    bool found = false;
    for (int i = 0; i < n; i++) {
        if (a[i] == k) {
            found = true;
            break;
        }
    }
    
    if (found) {
        cout << "True" << endl;
    } else {
        cout << "False" << endl;
    }
    
    return 0;
}
